{
  "buildCommand": "cd working-backwards-assistant && npm install && CI=false npm run build",
  "outputDirectory": "working-backwards-assistant/build",
  "installCommand": "cd working-backwards-assistant && npm install",
  "framework": "create-react-app",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ]
} 