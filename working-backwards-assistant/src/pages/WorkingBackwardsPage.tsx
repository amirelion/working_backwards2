import React from 'react';
import WorkingBackwardsPage from '../features/working-backwards';

/**
 * Page component that imports the refactored WorkingBackwardsPage from the features directory
 */
const WorkingBackwardsPageWrapper: React.FC = () => {
  return <WorkingBackwardsPage />;
};

export default WorkingBackwardsPageWrapper;